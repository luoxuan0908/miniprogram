# AI轻松英语 · 小程序发布上线操作指南

> 适用对象：个人开发者，首次发布小程序，零经验。
> 当前状态：代码已开发完成，云开发环境已配置，等待上线。

---

## 📋 发布前信息确认表

在微信后台提交时需要填写的信息，请逐项核对：

| 配置项 | 填写内容 | 位置 |
|--------|---------|------|
| **小程序名称** | `AI轻松英语` | 设置 → 基本设置 → 小程序名称 |
| **小程序简介** | `AI生成学习内容与音频，听写、回忆、识别多模式练词汇，艾宾浩斯曲线智能复习，碎片时间轻松学英语。` | 设置 → 基本设置 → 简介 |
| **小程序图标** | 240×240 PNG 图片（需准备） | 设置 → 基本设置 → 小程序头像 |
| **服务类目** | `教育服务 → 语言学习` | 设置 → 基本设置 → 服务类目 |
| **AppID** | `wx57a74d66c06421ad` | 设置 → 基本设置 → 账号信息 |

### 需要准备的文件

| 文件 | 规格 | 用途 |
|------|------|------|
| 小程序图标 | 240×240 px, PNG/JPG | 搜索结果展示、分享卡片头像 |
| TabBar 图标 × 4 | 81×81 px, PNG 透明背景 | 底部导航栏（首页选中/未选中、我的选中/未选中） |

---

## 第 0 步：注册申请小程序账号

> 如果你已经有小程序账号（AppID 为 `wx57a74d66c06421ad`），可以跳过这一步，直接到[第 1 步](#第-1-步登录微信公众平台)。

### 0.1 注册前准备

在开始注册之前，请准备好以下东西：

| 需要准备 | 说明 | 为什么需要 |
|----------|------|-----------|
| 📧 **一个未注册过微信公众平台的邮箱** | QQ 邮箱、163 邮箱、Gmail 等都可以 | 作为登录账号，每个邮箱只能注册一个微信公众平台账号 |
| 📱 **你自己的微信** | 已实名认证、绑定了本人银行卡的微信 | 作为小程序管理员，需要扫码验证身份 |
| 🪪 **身份证** | 本人二代身份证 | 填写实名信息 |
| 💳 **本人银行卡** | 绑定了你微信的银行卡 | 微信实名验证会校验 |
| 📞 **手机号** | 能接收短信的本人手机号 | 账号安全验证 |

> ⚠️ **一个关键区别：个人主体 vs 企业主体**
>
> | 能力 | 个人主体 | 企业/个体户主体 |
> |------|:---:|:---:|
> | 发布小程序 | ✅ | ✅ |
> | 使用云开发 | ✅ | ✅ |
> | 使用微信支付 | ❌ | ✅ |
> | 获取用户手机号 | ❌ | ✅ |
> | 微信认证（加蓝V） | ❌ 不开放 | ✅ 300元/年 |
> | 类目选择范围 | 有限 | 更广 |
> | 开通门槛 | 免费，身份证即可 | 需要营业执照 |
>
> **本指南按个人主体来操作**，这也是你当前项目的实际情况（AppID `wx57a74d66c06421ad` 属于个人主体，静默登录）。

### 0.2 开始注册

#### ① 打开注册页面

在浏览器打开：**https://mp.weixin.qq.com**

点击页面右上角的 **「立即注册」** 按钮。

#### ② 选择账号类型

注册页面会列出四种账号类型：
- 订阅号
- 服务号
- **小程序** ← 选这个
- 企业微信

点击「小程序」卡片，进入下一步。

#### ③ 填写注册邮箱

| 字段 | 填写内容 |
|------|---------|
| 邮箱 | 输入你的邮箱地址（如 `xxxxx@qq.com`） |
| 密码 | 设置登录密码（8 位以上，包含字母+数字+符号） |
| 确认密码 | 再输一遍密码 |
| 验证码 | 输入右侧图片中的验证码 |

勾选「我已阅读并同意《微信公众平台服务协议》」，点击「注册」。

#### ④ 激活邮箱

1. 微信会发送一封激活邮件到你填写的邮箱
2. 打开邮箱，找到「微信公众平台」发来的邮件
3. 点击邮件中的激活链接（或复制链接到浏览器打开）

> 💡 如果没收到邮件：检查垃圾邮件箱；如果超过 10 分钟还没收到，点「重新发送」。

#### ⑤ 选择主体类型

激活邮箱后，进入主体信息登记页面。

选择 **「个人」**，然后填写：

| 字段 | 填写内容 |
|------|---------|
| 身份证姓名 | 你的真实姓名 |
| 身份证号码 | 你的 18 位身份证号 |
| 管理员手机号 | 你正在使用的手机号，会收到短信验证码 |
| 短信验证码 | 手机收到的验证码 |

#### ⑥ 管理员微信扫码验证

填写完上面信息后，系统会生成一个二维码。

用你自己的微信扫码：
1. 手机上会弹出「微信公众平台」身份验证页面
2. 确认你的微信昵称和头像
3. 点击「确认」
4. 如果微信已实名且绑定了银行卡，验证会自动通过

> ⚠️ **常见失败原因：**
> - 微信号未绑定本人银行卡 → 先去微信钱包绑定一张银行卡
> - 身份证姓名与微信实名不一致 → 检查是否用了别人的身份证
> - 扫码次数超限 → 等 24 小时后再试

#### ⑦ 填写小程序信息

验证通过后，填写小程序基本信息：

| 字段 | 填写内容 |
|------|---------|
| 小程序名称 | `AI轻松英语`（此时可以先填，发布前还能改） |
| 小程序简介 | `AI生成学习内容与音频，听写、回忆、识别多模式练词汇，艾宾浩斯曲线智能复习，碎片时间轻松学英语。` |
| 小程序头像 | 先随便传一张 240×240 的图，后面可以换 |
| 服务类目 | `教育服务 → 语言学习` |

#### ⑧ 注册完成

点击「提交」后，小程序账号就创建成功了！

你会看到一个小程序的 Dashboard 页面，**记下 AppID**（在「开发 → 开发管理 → 开发设置」中可以找到，格式如 `wx57a74d66c06421ad`）。

> 📌 **你的项目已经有 AppID 了（`wx57a74d66c06421ad`），说明账号已注册。上面流程供参考/备用。**

### 0.3 注册后需要知道的事情

| 事项 | 说明 |
|------|------|
| 登录地址 | 以后每次管理小程序都去 https://mp.weixin.qq.com |
| 管理员微信 | 扫码登录只能用注册时那个微信，如果换了微信需要去「成员管理」添加 |
| AppID | 在「开发 → 开发管理 → 开发设置」里查看，用的最多的就是这个 |
| AppSecret | 也在「开发设置」里，API 调用用的密钥，绝对不能泄露 |
| 小程序名称修改 | 发布前可以随便改，发布后每年只能改 2 次 |
| 小程序简介修改 | 每个月能改 5 次 |

---

## 第 1 步：登录微信公众平台

### 1.1 打开管理后台

在浏览器打开：**https://mp.weixin.qq.com**

> 💡 注意：不是 `open.weixin.qq.com`（那是开放平台），也不是 `developers.weixin.qq.com`（那是开发者文档）。

### 1.2 扫码登录

用你注册小程序时绑定的管理员微信扫码。

### 1.3 进入管理后台后你能看到什么

登录后会看到一个 Dashboard（仪表盘）界面，左侧是菜单栏。与本次发布有关的菜单：

```
左侧菜单：
├── 首页（Dashboard）
├── 管理
│   ├── 版本管理 ★（上传、审核、发布在这里）
│   ├── 成员管理
│   └── 微信支付（个人主体不支持）
├── 开发
│   ├── 开发管理
│   │   ├── 开发设置 ★（AppID、服务器域名在这里）
│   │   └── 接口设置
│   └── 云开发
└── 设置 ★
    ├── 基本设置（名称、简介、图标、类目在这里）
    ├── 功能设置
    ├── 第三方设置
    └── 服务内容声明 ★（隐私协议在这里）
```

---

## 第 2 步：完善小程序基本信息

### 2.1 进入基本设置页面

左侧菜单 → **设置** → **基本设置**

### 2.2 逐一填写

#### ① 小程序名称

- 点击名称旁的「修改」链接
- 输入：`AI轻松英语`
- 注意事项：
  - 名称不能与已发布的小程序重名
  - 个人主体的小程序，名称中不能包含「公众号」「服务号」等词
  - 发布后每年可修改 2 次（不是无限改，想好再定）
  - 如果提示「名称已被占用」，可以尝试加后缀如 `AI轻松英语Pro`，但尽量不加

#### ② 小程序简介

- 点击简介旁的「修改」
- 输入：

```
AI生成学习内容与音频，听写、回忆、识别多模式练词汇，艾宾浩斯曲线智能复习，碎片时间轻松学英语。
```

- 字数要求：4-120 字，上面这段是 **53 字**，符合要求
- 注意：简介里不要放网址、电话、微信号等联系方式（审核会拒）

#### ③ 小程序图标

- 点击头像区域的「修改」
- 上传一张 **240×240 px** 的图片
- 要求：清晰、简洁、在小尺寸下依然可辨识
- 建议：耳机/声波 + 文字「词」或字母元素的设计
- 这里我稍后可以帮你生成

#### ④ 服务类目

- 点击「服务类目」区域的「添加类目」
- 搜索或选择：`教育服务 → 语言学习`

  步骤：
  1. 先点下拉选择「教育服务」
  2. 再点第二个下拉选择「语言学习」
  3. 点击「确定」

- 为什么选「语言学习」而不是「在线教育」？
  - 「语言学习」和你产品（英语词汇练习）完全对口
  - 个人主体选「语言学习」不需要提交额外的资质证明
  - 「在线教育」类目审核更严格，可能需要办学许可证

> ⚠️ 类目选错会导致审核被拒，务必选对。

### 2.3 暂时不用管的其他设置

- **功能设置**：不需要操作（隐私协议在另一个菜单）
- **第三方设置**：不需要操作
- **开发设置**：AppID 和云环境应该已经自动关联，不需要手动改（除非审核报告说域名不合法）

---

## 第 3 步：配置隐私保护指引 ⚠️ 必做，否则审核必拒

### 3.1 为什么必须做

微信从 2023 年 9 月起强制要求所有小程序设置隐私保护指引。你的小程序涉及：
- ✅ 获取用户头像、昵称 → 必须声明「用户信息」
- ✅ 调用云开发 → 不需要声明（微信云开发是免隐私声明的）

### 3.2 操作步骤

1. 左侧菜单 → **设置** → **服务内容声明** → **用户隐私保护指引**
2. 点击「去完善」
3. 勾选你的小程序使用的用户信息类型：

```
✅ 用户信息（昵称、头像）
❌ 位置信息（不需要）
❌ 手机号（个人主体无法获取，不需要）
❌ 相册/相机（你没有用到图片上传功能，不需要）
❌ 麦克风（没有录音功能，不需要）
```

4. 填写每一项的「用途说明」：

| 勾选项 | 用途说明 |
|--------|---------|
| 用户信息（昵称、头像） | 用于在「我的」页面展示用户身份，区分不同用户的学习数据。 |

5. 点击「提交」
6. 通常立即生效，无需等待审核

---

## 第 4 步：替换 TabBar 图标

### 4.1 现在的状态

当前使用的 TabBar 图标是占位的纯色 PNG 图片，位于：
- `miniprogram/images/tab-words.png`（首页 未选中）
- `miniprogram/images/tab-words-active.png`（首页 选中）
- `miniprogram/images/tab-review.png`（我的 未选中）
- `miniprogram/images/tab-review-active.png`（我的 选中）

### 4.2 需要准备什么

4 个 PNG 文件，规格要求：

| 项目 | 要求 |
|------|------|
| 尺寸 | **81 × 81 px** |
| 格式 | PNG（必须透明背景） |
| 大小 | 每个文件建议 < 40KB |
| 颜色 | 未选中：#8b8f99（灰色）；选中：#111217（深黑） |
| 注意 | 图片中不要有大面积文字或复杂细节 |

### 4.3 替换方法

你有两个选择：

**方案 A：用在线图标库**

1. 去 [iconfont.cn](https://iconfont.cn) 搜索「首页」和「用户」图标
2. 下载 81×81 的 PNG，按上述颜色导出两份（灰色 + 深色）
3. 改名为 `tab-words.png` / `tab-words-active.png` / `tab-review.png` / `tab-review-active.png`
4. 放进 `miniprogram/images/` 目录替换原文件

**方案 B：让我帮你生成（推荐）**

我可以直接帮你生成 4 个符合规范的 SVG 图标，然后转换为 PNG 替换现有文件。

### 4.4 app.json 的 TabBar 配置（已正确，不用改）

```json
"tabBar": {
  "list": [
    { "pagePath": "pages/index/index",    "text": "首页", "iconPath": "images/tab-words.png",            "selectedIconPath": "images/tab-words-active.png" },
    { "pagePath": "pages/review/review",  "text": "我的",  "iconPath": "images/tab-review.png",           "selectedIconPath": "images/tab-review-active.png" }
  ]
}
```

---

## 第 5 步：确认云函数是否已部署最新版本

### 5.1 检查清单

在开发者工具中 → 云开发控制台 → 云函数，确认以下云函数已部署：

| 云函数 | 状态 | 超时时间 | 说明 |
|--------|------|---------|------|
| login | ✅ | 3 秒即可 | 静默登录 |
| generateContent | ✅ | 30 秒 | DeepSeek 生成学习内容 |
| tts | ✅ | 30 秒 | Qwen-TTS 语音合成 |
| translateSegment | ✅ | 30 秒 | 逐段翻译 |
| parseResource | ✅ | 30 秒 | 文档解析 |
| uploadIdiomAudio | ✅ | 30 秒 | 习语音频上传 |

### 5.2 环境变量确认

在云开发控制台 → 云函数 → 每个函数 → 函数配置 → 环境变量，确认：

| 云函数 | 环境变量 |
|--------|---------|
| generateContent | `DEEPSEEK_API_KEY` |
| tts | `DASHSCOPE_API_KEY` |
| translateSegment | `DEEPSEEK_API_KEY` |

### 5.3 云数据库权限确认

云开发控制台 → 数据库 → 集合权限设置，确认以下集合的权限：

| 集合 | 读取权限 | 写入权限 |
|------|---------|---------|
| words | 所有用户可读 | 仅创建者可写 |
| resources | 所有用户可读 | 仅创建者可写 |
| courses | 所有用户可读 | 仅创建者可写 |
| userProfiles | 所有用户可读 | 仅创建者可写 |
| preferences | 所有用户可读 | 仅创建者可写 |

> 💡 这些权限设置只影响「客户端直接读写数据库」。你的云函数调用不受此限制。

---

## 第 6 步：在开发者工具中上传代码

### 6.1 上传前检查

在微信开发者工具中：
1. 确认左上角「详情」中 AppID 是 `wx57a74d66c06421ad`
2. 确认云函数都已「上传并部署：云端安装依赖」
3. 点击「编译」→ 在模拟器中快速过一遍所有页面，确认没有白屏或报错
4. admin 页面如果还不想暴露给用户，可以在 `app.json` 的 `pages` 数组中临时移除

### 6.2 上传操作

1. 在开发者工具右上角，点击 **「上传」** 按钮
2. 弹出对话框中填写：

| 字段 | 填写内容 |
|------|---------|
| 版本号 | `1.0.0` |
| 项目备注 | `首次发布：AI生成内容与音频，多模式听力练习，艾宾浩斯科学复习` |

3. 点击「上传」
4. 等待进度条走完，提示「上传成功」

### 6.3 上传后去哪儿看

上传成功后，回到微信公众平台：
- 左侧菜单 → **管理** → **版本管理**
- 你会看到刚才上传的版本，状态为「开发版本」

---

## 第 7 步：提交审核

### 7.1 进入提审页面

在「版本管理」页面，找到刚才上传的「开发版本」，点击右侧的 **「提交审核」** 按钮。

### 7.2 填写审核信息

#### ① 配置功能页面

审核员需要实际体验你的小程序。选择以下页面：

| 重要性 | 页面 | 为什么要展示 |
|--------|------|------------|
| 必选 | `pages/index/index`（首页） | 让审核员看到功能入口 |
| 必选 | `pages/courses/courses`（课程列表） | 展示核心学习内容 |
| 建议 | `pages/course-detail/course-detail` | 展示课程详情 |
| 建议 | `pages/review/review`（我的） | 展示个人中心和统计 |

#### ② 测试账号（重要！）

审核员需要登录才能看到完整功能。你用的是静默登录，所以：

- 如果你的小程序**不需要用户手动登录**（打开就自动 wx.login + 云函数 login），则**不需要提供**测试账号
- 如果你的小程序有**需要点击才能登录**的流程，则需要提供测试账号信息

你的情况是：打开小程序自动静默登录，所以**不需要额外提供测试账号**。

但在「测试账号」一栏写清楚：

```
本小程序使用微信静默登录，打开即自动完成登录，无需手动输入账号密码。审核员使用自己的微信扫码即可完整体验全部功能。
```

#### ③ 版本描述（审核员会看）

```
AI轻松英语是一款AI驱动的英语单词学习工具。
核心功能：
1. 多种练习模式：听写、回忆、识别、语境补全，多维度强化记忆。
2. AI内容生成：输入单词，AI自动生成释义、例句、搭配等学习资料。
3. TTS语音合成：所有学习内容配备AI语音，支持听力练习。
4. 艾宾浩斯复习：系统自动记录学习数据，按科学曲线安排复习。
5. 课程系统：内置结构化英语课程，系统化学习。

技术栈：微信云开发 + DeepSeek AI + 阿里云TTS
```

### 7.3 提交

填写完毕后，点击「提交审核」。

### 7.4 审核需要多久

- 正常情况：**1-3 个工作日**
- 高峰期（节假日、周末前）：可能延长到 5-7 个工作日
- 你的小程序属于「教育-语言学习」类目，是常规类目，审核速度通常较快

### 7.5 审核中你能做什么

- 审核中不能修改代码，不能重新上传
- 你可以加急：在「版本管理」页面，审核中的版本右侧有「加急」按钮。每个小程序每月有 1 次加急机会。

---

## 第 8 步：审核结果处理

### 8.1 如果审核通过 ✅

1. 在「版本管理」页面，你会看到状态变为「审核通过」
2. 点击「提交发布」
3. 选择发布模式：

| 模式 | 说明 | 建议 |
|------|------|------|
| **全量发布** | 所有用户立刻可以搜到和使用 | 如果你有用户基础或想立刻公开 |
| **灰度发布** | 按 5%→15%→30%→50%→100% 逐步放大 | 推荐首次发布用这个，先验证稳定性 |

4. 发布后，大约 **5-30 分钟** 后，微信搜索「AI轻松英语」即可找到你的小程序

### 8.2 如果审核被拒 ❌

常见的拒绝原因和解决办法：

| 拒绝原因 | 怎么修 |
|---------|--------|
| 「服务类目不明确」 | 检查是否选了「教育服务 → 语言学习」，不要选「工具」或「信息查询」 |
| 「功能不完整」 | 检查是否有空白页面，admin 页面是否已移除 |
| 「涉及用户隐私但未声明」 | 回到第 3 步，确认隐私保护指引已提交 |
| 「需要登录但未提供测试账号」 | 在审核描述中补充静默登录说明 |
| 「小程序名称与功能不符」 | 名称是「AI轻松英语」，功能确实是学词汇的，通常不会拒 |
| 「存在测试数据」 | 检查数据库中是否有 `test` `abc` `123` 这类测试单词，建议清掉或隐藏 |

被拒后：
1. 根据拒绝原因修改代码或后台设置
2. 重新在开发者工具中上传新版本
3. 再次提交审核

---

## 第 9 步：发布后验证

### 9.1 搜索验证

1. 打开微信 → 发现 → 搜一搜
2. 搜索 `AI轻松英语`
3. 应该能看到你的小程序出现在搜索结果中

### 9.2 功能验证

用另一台手机（非开发者微信）扫码或搜索进入，验证：
- 打开小程序，自动静默登录是否正常
- 添加单词 → 生成AI内容 → 音频播放是否正常
- 课程列表 → 课程详情 → 课程项详情是否正常
- 每日训练 → 听写/回忆/识别/语境四种模式是否正常
- 「我的」页面头像昵称展示是否正常

### 9.3 分享功能验证

在你的小程序页面中，点击右上角「···」→ 「转发」，确认分享卡片上有：
- ✅ 小程序图标
- ✅ 小程序名称「AI轻松英语」
- ✅ 转发标题

---

## 第 10 步：长期运营

### 10.1 获取小程序码

发布后，在微信公众平台 → **设置** → **基本设置** → 小程序码，可以：
- 下载普通二维码（永久有效）
- 下载小程序码（圆形菊花码，永久有效）
- 下载带参数的小程序码（可以做不同来源的统计）

### 10.2 数据监控

在微信公众平台 → 首页 Dashboard，可以查看：
- 累计访问用户数
- 日活跃用户
- 新增用户
- 页面访问分析

### 10.3 迭代发布

后续修改代码后：
1. 在开发者工具中修改版本号（如 `1.0.1`）
2. 点击「上传」
3. 在后台「版本管理」→「开发版本」→「提交审核」
4. 审核通过后发布
5. 每次迭代都需要重新审核（大约 1-3 天）

---

## 📌 你现在需要做的事（按优先级排序）

| 优先级 | 任务 | 状态 |
|--------|------|------|
| 🔴 P0 | 准备小程序图标（240×240） | ⬜ 待做 |
| 🔴 P0 | 替换 TabBar 图标（4个81×81 PNG） | ⬜ 待做 |
| 🟡 P1 | 登录 mp.weixin.qq.com 完善基本信息 | ⬜ 待做 |
| 🟡 P1 | 配置隐私保护指引 | ⬜ 待做 |
| 🟡 P1 | 确认云函数已部署 + 环境变量正确 | ⬜ 待确认 |
| 🟢 P2 | 开发者工具中上传代码 | ⬜ 待做 |
| 🟢 P2 | 提交审核 | ⬜ 待做 |
| 🟢 P3 | 审核通过后发布 | ⬜ 待做 |

---

> 需要我帮你生成小程序的图标和 TabBar 图标吗？
